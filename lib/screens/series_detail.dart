import 'package:flutter/material.dart';

class SeriesDetail extends StatelessWidget {
  final Map data;

  const SeriesDetail({required this.data, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
